package com.example.bucketplace.src.main.store.reaper

import android.os.Bundle
import android.view.View
import com.example.bucketplace.R
import com.example.bucketplace.config.BaseFragment
import com.example.bucketplace.databinding.FragmentMainStoreReaperBinding

class MainStoreReaperFragment :
    BaseFragment<FragmentMainStoreReaperBinding>(
        FragmentMainStoreReaperBinding::bind,
        R.layout.fragment_main_store_reaper
    ) {
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}